return {
    settings = {
        ty = {
            diagnosticMode = "workspace",
        },
    },
    root_markers = { "ty.toml", ".venv", "pyproject.toml", "setup.py", "setup.cfg", "requirements.txt", ".git" },
}
